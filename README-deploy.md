The server side application uses render.com as it's hosting service.

##Deploy Steps

-   Merge code to `production` branch
-   Go to render.com -> dashboard -> toastyghost-server
-   Select "Manual deploy" and "Deploy latest commit"
